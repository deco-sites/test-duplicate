import {
  default as Component,
  Props,
} from "$store/components/institutional/InstitutionalBody.tsx";

/**
 * @title Corpo do Institucional
 */
export default function Section(props: Props) {
  return <Component {...props} />;
}
